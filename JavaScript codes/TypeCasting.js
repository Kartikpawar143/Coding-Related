var contact = "989898989";//string
var convertedContact = +contact; // + is used to convert string to numeber.

var isLogedIn = false; //boleean
var convertedBol = isLogedIn + ""; // also .toString can be used.



console.log(convertedBol)
console.log(isLogedIn);
console.log(contact);
console.log(convertedContact);